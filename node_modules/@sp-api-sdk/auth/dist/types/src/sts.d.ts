import { type Credentials } from '@aws-sdk/client-sts';
interface Role {
    arn: string;
    sessionName: string;
}
export interface SecurityTokenServiceParameters {
    accessKeyId: string;
    secretAccessKey: string;
    sessionToken?: string;
    role: Role;
    region?: string;
}
export declare class SecurityTokenService {
    readonly accessKeyId: string;
    readonly secretAccessKey: string;
    readonly sessionToken?: string;
    readonly role: Role;
    readonly region?: string;
    private _credentials?;
    constructor(parameters: SecurityTokenServiceParameters);
    /**
     * Get the AWS credentials from STS
     */
    getCredentials(): Promise<Credentials>;
    /**
     * Fetch new AWS credentials from STS
     */
    protected fetchCredentials(): Promise<Credentials>;
}
export {};
